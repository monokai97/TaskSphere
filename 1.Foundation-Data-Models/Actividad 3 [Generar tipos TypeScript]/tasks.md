# Tasks: Generar tipos TypeScript

## Dependencias
- Actividad 2 completada (`payload.config.ts` registra las 5 colecciones)

---

## Hito 3.1: Ejecutar generación

- [ ] Ejecutar `pnpm generate:types`
- [ ] Verificar que el comando termina con código 0 (sin errores)

## Hito 3.2: Verificar interfaces

- [ ] Abrir `src/payload-types.ts`
- [ ] Confirmar que exporta `interface Task` con campos: id, title, description, status, important, dueDate, list, guestId, sortOrder, completedAt, subtasks, createdAt, updatedAt
- [ ] Confirmar que exporta `interface List` con campos: id, name, icon, color, guestId, isDefault, sortOrder, createdAt, updatedAt
- [ ] Confirmar que exporta `interface TaskLog` con campos: id, task, guestId, operation, previousState, newState, timestamp
- [ ] Confirmar que exporta `interface GuestSession` con campos: id, guestId, createdAt, lastActiveAt, expiresAt, locale, theme, notificationsEnabled, integrations, focusSettings
- [ ] Confirmar que exporta `interface FocusSession` con campos: id, guestId, duration, completed, completedAt, date
- [ ] Verificar que `list` en Task es de tipo `string | List` (relationship)
- [ ] Verificar que `task` en TaskLog es de tipo `string | Task` (relationship)
- [ ] Verificar que `operation` en TaskLog es de tipo `'CREATE' | 'UPDATE' | 'DELETE'`
- [ ] Verificar que `locale` en GuestSession es de tipo `'es' | 'en' | null`
- [ ] Verificar que `theme` en GuestSession es de tipo `'light' | 'dark' | 'system' | null`

## Hito 3.3: Type-check

- [ ] Ejecutar `npx tsc --noEmit`
- [ ] Verificar 0 errores de tipo
- [ ] Prestar atención a errores en `src/collections/*.ts` (los schemas deben ser compatibles con los tipos generados)

## Verificación

- [ ] `src/payload-types.ts` tiene el header "This file was automatically generated by Payload. DO NOT MODIFY IT BY HAND."
- [ ] `pnpm lint` pasa sin errores (si el comando está configurado)
- [ ] Los tipos se pueden importar: `import type { Task } from '@/payload-types'` resuelve correctamente
